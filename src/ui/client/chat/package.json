{
  "name": "@nanoclaw/ui-chat",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser chat UI for NanoClaw. Bundled with esbuild; output checked in at dist/app.js.",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.tsx --bundle --format=esm --target=es2022 --sourcemap --outfile=dist/app.js",
    "build:no-typecheck": "esbuild src/index.tsx --bundle --format=esm --target=es2022 --sourcemap --outfile=dist/app.js",
    "watch": "esbuild src/index.tsx --bundle --format=esm --target=es2022 --sourcemap --outfile=dist/app.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@preact/signals": "2.0.1",
    "highlight.js": "^11.11.1",
    "marked": "18.0.4",
    "path-to-regexp": "^8.4.2",
    "preact": "10.25.4"
  },
  "devDependencies": {
    "esbuild": "0.24.2",
    "typescript": "^5.9.3"
  }
}
